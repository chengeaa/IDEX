---
title: "IDEX cleaning"
author: "me"
date: "March 9, 2017"
output: html_document
---

```{r setup, include=FALSE}
require(readr)
```

```{r}
revenue = read_csv("C://Users/Erik Cheng/IDEX/7yr.csv")
#so uh wth is the difference between consulatants and consultants2
```
```{r}
names = gsub(" -.*", "", revenue$`Donation Name`)
nonrep = unique(names)
repped = function(names){
  repped = character()
  for (i in names) {
    if (length(grep(i, names)) > 1){
      repped = c(repped, i)
    }
  }
  return (unique(repped))
}
repeats = repped(names)
```

